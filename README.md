# entitlements-sync

LF → UC entitlements sync engine. CPP POC (Plan 1: engine core).

See `docs/superpowers/specs/2026-05-06-glue-uc-entitlements-sync-design.md`
in the parent `asq` repo for design context.

## Quickstart

```bash
uv venv && source .venv/bin/activate
uv pip install -e ".[dev]"
pytest
```
